# Handoff：Legacy Frontend Enhancement v3 — 领任务开工

> 生成时间：2026-08-06 21:15:05  
> 仓库：`T:/Infinite-Canvas`  
> 分支：`feature/original-frontend-optimization`（tip：`e9ae0b5` UI/ESM/design-system 已合）  
> **下一会话目标：从 v3 issues 领任务并开始实现（优先无阻塞 AFK 切片）**

---

## 1. 一句话状态

产品主壳已定：**Legacy Frontend（`static/`）**，不再主推 Studio V2 React 替换。  
本会话完成能力盘点 → **PRD v3** → **14 条垂直切片 issues**。**尚未写业务代码实现 v3。**

---

## 2. 必读产物（勿复述全文）

| 产物 | 路径 |
|------|------|
| **PRD（范围与模块 M1–M10）** | `.scratch/legacy-frontend-enhancement-v3/PRD.md` |
| **Issues 01–14** | `.scratch/legacy-frontend-enhancement-v3/issues/` |
| ADR-0002 ESM | `docs/adr/0002-legacy-frontend-native-esm-refactoring.md` |
| 领域词 | `CONTEXT.md` |
| 问题追踪约定 | `docs/agents/issue-tracker.md`、`docs/agents/triage-labels.md` |
| 前序 UI 交接 | `.handoff/handoff-20260806-202710.md` |
| V2 仅契约对照（非主壳） | `.scratch/studio-v2-mvp/`、`API/v2/*`、`studio-v2/` |

**决策摘要（已写入 PRD，执行时遵守）：**

- 壳 = Legacy；`/api/v2` 复用后端；`static/` 当前 **零** `/api/v2` 调用。  
- ESM 六件套在 `static/js/modules/smart-canvas/` 是**脚手架**，业务仍在巨型 `smart-canvas.js`。  
- 级联/模板等**旧逻辑已存在**，v3 是抽出+面板+增强+接 Agent/资产，不是从 0 发明级联。  
- 必测纯逻辑：**M1 / M3 / M4 / M6 config**（见 PRD 测试决策）。  
- 禁：main.py 堆业务、第三套 Task 协议、Legacy 强制上打包器、把 studio-v2 设默认入口。

---

## 3. 下一会话怎么领任务

### 3.1 可立即并行（无阻塞）

| Issue | 内容 |
|-------|------|
| [01](../.scratch/legacy-frontend-enhancement-v3/issues/01-m3-node-run-status-bus.md) | M3 Status Bus + 徽章/连线接线 |
| [02](../.scratch/legacy-frontend-enhancement-v3/issues/02-m1-cascade-tracker-logic.md) | M1 Cascade 纯逻辑 + 单测 |
| [04](../.scratch/legacy-frontend-enhancement-v3/issues/04-m2-prompt-template-hub.md) | M2 提示词模板中心 |
| [06](../.scratch/legacy-frontend-enhancement-v3/issues/06-m4-legacy-v2-api-client.md) | M4 V2 Client（Agent 链地基） |
| [13](../.scratch/legacy-frontend-enhancement-v3/issues/13-m10-platform-gaps.md) | Avatar 白名单 + 设置 CLI 探测 |
| [14](../.scratch/legacy-frontend-enhancement-v3/issues/14-v3-smoke-and-release-gate.md) | 冒烟文档骨架可先写 |

**推荐开工组合（一条主链 + 一条体验链）：**

```text
A. 06 V2 Client →（随后）07 Center → 08 Dock → 09 Agent Node
B. 01 Status Bus + 02 Cascade Logic → 03 追踪面板
（04 模板可与 A/B 并行）
```

若只开一个代理：**优先 06**（解锁 Agent P0）或 **01+02**（旧画布增强、不依赖 v2）。

### 3.2 依赖链（完整）

```text
01 ─┬─► 03
02 ─┘
01 ──► 12（ESM 深焊）
04 ──► 05
06 ──► 07 ──► 08 ──► 09
06 ──► 10、11
13 独立
14 文档即开；完整勾选建议 03+05+09 后
```

### 3.3 执行习惯

- 每切片：读 issue 验收标准 → 实现 → 跑该片要求的测 → 手测相关冒烟点。  
- 用户规则：改代码前若全局 CLAUDE 要求确认——**以用户本会话「开始领任务执行」为准可直接实现**；高风险（删旧资产数据、改后端契约）仍先说明。  
- git：只提交切片相关文件；勿动用户本地未提交的 `API/.env`、`data/*`、随意 `static` 无关脏文件。  
- Python：`.venv/Scripts/python.exe`；后端 3888；shell 偏好 `rtk` 前缀。  
- 样式：优先 `static/css/design-system.css`；cursor 禁止逗号多关键字。

---

## 4. 代码现实检查（避免重复劳动）

- **级联**：`static/js/smart-canvas.js` 已有 `runSmartCascade` / `canRunSmartCascade` / `conn-cascade-*`；`canvas.js` 亦有 cascade 集。02/03 = 抽取+投影+面板，对齐现网语义。  
- **模板**：已有 `promptTemplate*` 与模板 modal；04 = 产品化增强。  
- **V2 后端**：Agent/Asset/Generation 已在 `API/v2/`；对照 `studio-v2/src/features/agents/` 实现 Client/UI，**移植语义到 static**，不要再开 React 主壳。  
- **Avatar**：`static/js/asset-manager.js` 中 `AVATAR_SUPPORTED_PLATFORMS = apimart, volcengine`。

---

## 5. 环境（无密钥）

| 项 | 值 |
|----|-----|
| 后端 | `.venv/Scripts/python.exe main.py` → 3888 |
| 旧前端 | FastAPI 托管 `static/` |
| V2 DB | `data/studio-v2/studio.db` |
| Agent 真机 | 本机 CLI（Claude/Pi/omp 等）— 无 CLI 仍可测 UI + Probe 失败态 |
| 勿提交 | `API/.env`、密钥、个人路径 |

（历史 handoff 中出现的本地 LLM base/key **不要写入代码或新文档**；需要时用户本机配置。）

---

## 6. 建议技能（下次会话）

- **implement / 直接编码**：按 issue 验收落地  
- **tdd**：01/02/06/09 的纯逻辑先红后绿  
- **diagnose**：级联回归或 v2 对接失败时  
- **buffalo-frontend**（若团队规范适用）：Legacy 模块职责与行数  
- **api-tester**：07+ 联调 `/api/v2/agent-*`  
- **planning-with-files**：并行多 issue 时  
- **review**：切片提交后 Spec + Standards  
- **勿再** to-prd/to-issues（除非改范围）

---

## 7. 下一会话开场清单（复制即用）

1. 读本 handoff + 目标 issue 全文 + PRD 对应模块段。  
2. 选 **06** 或 **01+02**（或用户指定）。  
3. 实现并满足验收标准与必测项。  
4. 更新 issue 验收勾选（若流程需要）/ 简短进度。  
5. 询问是否提交（默认不自动 commit，除非用户要求）。

**不要做：** 重开 Studio V2 主壳发布、重写 PRD、扩大到 Event Hub/多租户。
