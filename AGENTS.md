## 代理技能

### 问题追踪器

问题作为本地 Markdown 存储在 `.scratch/<feature>/` 下。参见 `docs/agents/issue-tracker.md`。

### 分类标签

五个规范角色使用默认标签名（needs-triage、needs-info、ready-for-agent、ready-for-human、wontfix）。参见 `docs/agents/triage-labels.md`。

### 领域文档

单一上下文：根目录 CONTEXT.md + `docs/adr-*.md` 文件（本仓库 ADR 扁平存放于 docs/）。参见 `docs/agents/domain.md`。
